# 파이썬 2일차 강의 교안

## 강의 정보
- **강의 시간**: 4시간 (240분)
- **세션 구성**: 이론(10분) + 실습(10분) + 해설(5분) = 25분/세트
- **총 세션**: 9개
- **주제**: 컬렉션 자료형과 연산자 완전 정복

---

## 📋 목차

1. [세션 1: 컬렉션 자료형 기초](#세션-1-컬렉션-자료형-기초-) (25분)
2. [세션 2: 타입 변환](#세션-2-타입-변환-) (25분)
3. [세션 3: input() 함수](#세션-3-input-함수-) (25분)
4. [세션 4: f-string 포매팅](#세션-4-f-string-포매팅-) (25분)
5. [세션 5: 산술 연산자](#세션-5-산술-연산자-) (25분)
6. [세션 6: 비교 연산자](#세션-6-비교-연산자-) (25분)
7. [세션 7: 논리 연산자](#세션-7-논리-연산자-) (25분)
8. [세션 8: 연산자 우선순위](#세션-8-연산자-우선순위-) (25분)
9. [세션 9: 삼항 연산자](#세션-9-삼항-연산자-) (25분)

---

## 세션 1: 컬렉션 자료형 기초 ★★★★★

### 📖 이론 (10분)

#### 개념 설명
여러 값을 하나로 묶어 관리하는 자료형입니다. **리스트(list)**, **튜플(tuple)**, **딕셔너리(dict)**가 핵심입니다.

#### 주요 개념
- **리스트**: 순서 있음, 수정 가능, 대괄호 `[]`
- **튜플**: 순서 있음, 수정 불가, 소괄호 `()`
- **딕셔너리**: 키-값 쌍, 중괄호 `{}`

#### 실무 활용 사례
- 상품 목록 관리 (리스트)
- 좌표, RGB 값 (튜플)
- 사용자 정보 (딕셔너리)

#### 코드 예시
```python
# 예시 1: 리스트 (수정 가능)
fruits = ["사과", "바나나", "포도"]
print(fruits[0])  # "사과"
fruits.append("딸기")
print(fruits)  # ["사과", "바나나", "포도", "딸기"]

# 장바구니 총액 계산
cart = [
    {"name": "키보드", "price": 45000},
    {"name": "마우스", "price": 25000}
]
total = sum(item["price"] for item in cart)
print(f"총액: {total:,}원")

# 예시 2: 튜플 (수정 불가)
point = (10, 20)
x, y = point  # 언패킹
print(x, y)  # 10 20

# RGB 색상
color = (255, 128, 0)
# color[0] = 200  # ❌ TypeError (수정 불가)

# 예시 3: 딕셔너리 (키-값 쌍)
user = {
    "name": "김파이썬",
    "age": 28,
    "email": "python@example.com"
}
print(user["name"])  # "김파이썬"
user["age"] = 29  # 수정 가능

# 안전한 접근
email = user.get("phone", "없음")  # KeyError 방지
print(email)  # "없음"
```

---

### 💻 실습 (10분)

**[실습 파일: session1_collections_practice.py](./session1_collections_practice.py)**

---

### ✅ 해설 (5분)

**[해설 파일: session1_collections_solution.py](./session1_collections_solution.py)**

---

## 세션 2: 타입 변환 ★★★★★

### 📖 이론 (10분)

#### 개념 설명
한 자료형을 다른 자료형으로 변환하는 것입니다. `input()`은 항상 문자열을 반환하므로 타입 변환이 필수입니다.

#### 주요 개념
- **int()**: 정수로 변환
- **float()**: 실수로 변환
- **str()**: 문자열로 변환

#### 실무 활용 사례
- 사용자 입력 처리 (숫자 입력)
- 데이터 정제 (CSV 파일 읽기)
- API 응답 처리 (JSON 파싱)

#### 코드 예시
```python
# 예시 1: int() - 정수로 변환
age_str = "28"
age = int(age_str)
print(age + 2)  # 30

# 실수 → 정수 (소수점 버림)
height = int(175.8)
print(height)  # 175

# 예시 2: float() - 실수로 변환
price = float("3.14")
print(price * 2)  # 6.28

# 정수 → 실수
count = float(10)
print(count)  # 10.0

# 예시 3: str() - 문자열로 변환
age = 28
message = "나이: " + str(age) + "세"
print(message)  # "나이: 28세"

# f-string은 자동 변환
message = f"나이: {age}세"
print(message)  # "나이: 28세"

# 실무 패턴: 사용자 입력 처리
age_input = input("나이: ")  # "28" (문자열)
age = int(age_input)  # 28 (정수)

if age >= 18:
    print("성인입니다")
```

---

### 💻 실습 (10분)

**[실습 파일: session2_type_conversion_practice.py](./session2_type_conversion_practice.py)**

---

### ✅ 해설 (5분)

**[해설 파일: session2_type_conversion_solution.py](./session2_type_conversion_solution.py)**

---

## 세션 3: input() 함수 ★★★★★

### 📖 이론 (10분)

#### 개념 설명
사용자로부터 키보드 입력을 받는 함수입니다. **항상 문자열(str)**을 반환합니다.

#### 주요 개념
- **반환값**: 항상 문자열
- **줄바꿈**: Enter는 입력에 포함되지 않음
- **타입 변환**: 숫자 입력 시 int() 또는 float() 필요

#### 실무 활용 사례
- 메뉴 선택 시스템
- 계산기 프로그램
- 회원가입 정보 수집

#### 코드 예시
```python
# 예시 1: 기본 사용
name = input("이름을 입력하세요: ")
print(f"안녕하세요, {name}님!")

# 예시 2: 숫자 입력 (타입 변환 필수)
age_str = input("나이: ")  # "28" (문자열)
age = int(age_str)  # 28 (정수)
print(f"내년 나이: {age + 1}")

# 한 줄로
age = int(input("나이: "))

# 예시 3: 메뉴 시스템
print("===== 메뉴 =====")
print("1. 로그인")
print("2. 회원가입")
print("3. 종료")

choice = input("선택: ")

if choice == "1":
    print("로그인 화면으로 이동")
elif choice == "2":
    print("회원가입 화면으로 이동")
elif choice == "3":
    print("프로그램 종료")
else:
    print("잘못된 선택입니다")

# 실무 패턴: 입력 검증
while True:
    age_str = input("나이 (1-120): ")

    if not age_str.isdigit():
        print("숫자만 입력하세요")
        continue

    age = int(age_str)
    if 1 <= age <= 120:
        break
    else:
        print("올바른 나이를 입력하세요")

print(f"입력된 나이: {age}세")
```

---

### 💻 실습 (10분)

**[실습 파일: session3_input_practice.py](./session3_input_practice.py)**

---

### ✅ 해설 (5분)

**[해설 파일: session3_input_solution.py](./session3_input_solution.py)**

---

## 세션 4: f-string 포매팅 ★★★★★

### 📖 이론 (10분)

#### 개념 설명
변수를 문자열에 쉽게 삽입하는 방법입니다. Python 3.6+에서 가장 권장하는 방식입니다.

#### 주요 개념
- **기본**: `f"변수: {변수명}"`
- **포맷 지정**: `{값:,}` (천 단위), `{값:.2f}` (소수점)
- **정렬**: `{값:>10}` (우측 정렬)

#### 실무 활용 사례
- 가격 표시 (천 단위 쉼표)
- 리포트 생성 (표 형식)
- 로그 메시지 (포맷 통일)

#### 코드 예시
```python
# 예시 1: 기본 f-string
name = "김파이썬"
age = 28

print(f"이름: {name}, 나이: {age}")
print(f"내년 나이: {age + 1}")  # 표현식 가능

# 예시 2: 숫자 포맷
price = 1234567
pi = 3.14159

print(f"{price:,}원")  # 1,234,567원 (천 단위 쉼표)
print(f"{pi:.2f}")  # 3.14 (소수점 2자리)
print(f"{price:>15,}원")  # "      1,234,567원" (우측 정렬)

# 퍼센트
rate = 0.856
print(f"{rate:.1%}")  # 85.6%

# 예시 3: 테이블 형식 출력
students = [
    {"name": "김철수", "score": 85},
    {"name": "이영희", "score": 92}
]

print(f"{'이름':^10} | {'점수':^10}")
print("-" * 25)
for student in students:
    print(f"{student['name']:^10} | {student['score']:^10}")

# 출력:
#    이름     |    점수
# -------------------------
#   김철수    |     85
#   이영희    |     92

# 실무 패턴: 리포트 생성
revenue = 5000000
cost = 3000000
profit = revenue - cost

print(f"""
{'=' * 40}
           매출 리포트
{'=' * 40}
매출: {revenue:>15,}원
비용: {cost:>15,}원
{'=' * 40}
이익: {profit:>15,}원
{'=' * 40}
""")
```

---

### 💻 실습 (10분)

**[실습 파일: session4_fstring_practice.py](./session4_fstring_practice.py)**

---

### ✅ 해설 (5분)

**[해설 파일: session4_fstring_solution.py](./session4_fstring_solution.py)**

---

## 세션 5: 산술 연산자 ★★★★★

### 📖 이론 (10분)

#### 개념 설명
숫자를 계산하는 연산자입니다. Python의 `/`는 항상 float을 반환합니다.

#### 주요 개념
- **기본**: `+`, `-`, `*`, `/` (항상 float)
- **정수 나눗셈**: `//` (몫), `%` (나머지)
- **거듭제곱**: `**`

#### 실무 활용 사례
- 가격 계산 (할인, 부가세)
- 페이지 계산 (올림)
- 짝수/홀수 판별

#### 코드 예시
```python
# 예시 1: 기본 연산
print(10 + 3)   # 13
print(10 - 3)   # 7
print(10 * 3)   # 30
print(10 / 3)   # 3.3333... (항상 float!)
print(10 // 3)  # 3 (몫)
print(10 % 3)   # 1 (나머지)
print(10 ** 3)  # 1000 (거듭제곱)

# 예시 2: 나머지 활용
# 짝수/홀수 판별
number = 7
if number % 2 == 0:
    print("짝수")
else:
    print("홀수")

# 5의 배수 확인
if number % 5 == 0:
    print("5의 배수")

# 예시 3: 실무 계산
# 가격 계산
price = 10000
quantity = 3
tax_rate = 0.1

subtotal = price * quantity
tax = subtotal * tax_rate
total = subtotal + tax

print(f"상품 금액: {subtotal:,}원")
print(f"부가세(10%): {tax:,}원")
print(f"총 금액: {total:,}원")

# 페이지 계산 (올림)
import math
total_items = 47
items_per_page = 10
total_pages = math.ceil(total_items / items_per_page)
print(f"총 페이지: {total_pages}")  # 5페이지
```

---

### 💻 실습 (10분)

**[실습 파일: session5_arithmetic_practice.py](./session5_arithmetic_practice.py)**

---

### ✅ 해설 (5분)

**[해설 파일: session5_arithmetic_solution.py](./session5_arithmetic_solution.py)**

---

## 세션 6: 비교 연산자 ★★★★★

### 📖 이론 (10분)

#### 개념 설명
두 값을 비교하여 True 또는 False를 반환합니다. 조건문의 핵심입니다.

#### 주요 개념
- **비교**: `>`, `<`, `>=`, `<=`, `==`, `!=`
- **체이닝**: `18 <= age < 65` (Python만 가능!)
- **멤버십**: `in`, `not in`

#### 실무 활용 사례
- 입력 검증 (범위 확인)
- 등급 시스템 (점수 구간)
- 파일 타입 확인

#### 코드 예시
```python
# 예시 1: 기본 비교
print(10 > 5)     # True
print(10 == 10)   # True
print(10 != 5)    # True

age = 25
print(age >= 18)  # True

# 문자열 비교
print("apple" < "banana")  # True (사전순)

# 예시 2: 체이닝 비교 (Python 특징!)
age = 25
if 18 <= age <= 65:
    print("성인 근로 가능 연령")

# Java 스타일 (가능하지만 비권장)
if age >= 18 and age <= 65:
    print("성인 근로 가능 연령")

# 예시 3: 멤버십 확인
# 리스트에 포함 여부
fruits = ["사과", "바나나", "포도"]
if "사과" in fruits:
    print("사과 있음")

if "딸기" not in fruits:
    print("딸기 없음")

# 문자열에 포함 여부
email = "user@example.com"
if "@" in email and "." in email:
    print("유효한 이메일 형식")

# 실무 패턴: 등급 시스템
score = 85

if score >= 90:
    grade = "A"
elif score >= 80:
    grade = "B"
elif score >= 70:
    grade = "C"
else:
    grade = "F"

print(f"학점: {grade}")
```

---

### 💻 실습 (10분)

**[실습 파일: session6_comparison_practice.py](./session6_comparison_practice.py)**

---

### ✅ 해설 (5분)

**[해설 파일: session6_comparison_solution.py](./session6_comparison_solution.py)**

---

## 세션 7: 논리 연산자 ★★★★★

### 📖 이론 (10분)

#### 개념 설명
여러 조건을 결합하는 연산자입니다. `and`, `or`, `not`을 사용합니다.

#### 주요 개념
- **and**: 모두 참이어야 참
- **or**: 하나라도 참이면 참
- **not**: 반대로
- **단축 평가**: 결과 확정 시 나머지 평가 안 함

#### 실무 활용 사례
- 로그인 검증 (아이디 AND 비밀번호)
- VIP 조건 (회원 OR 구매액)
- 권한 확인 (복합 조건)

#### 코드 예시
```python
# 예시 1: and 연산 (모두 참)
age = 25
has_license = True

if age >= 18 and has_license:
    print("운전 가능")

# 로그인 검증
username = "admin"
password = "1234"

if username == "admin" and password == "1234":
    print("로그인 성공")

# 예시 2: or 연산 (하나라도 참)
is_member = False
purchase_amount = 1200000

if is_member or purchase_amount >= 1000000:
    print("VIP 할인 적용")

# 주말 확인
day = "토요일"
if day in ["토요일", "일요일"]:  # 더 깔끔한 방법
    print("주말")

# 예시 3: not 연산
is_logged_in = False

if not is_logged_in:
    print("로그인이 필요합니다")

# 빈 값 확인
user_input = ""
if not user_input:
    print("입력값이 없습니다")

# 실무 패턴: 복합 조건
score = 85
attendance = 95
assignment_done = True

# 학점 A 조건: (점수 90 이상) 또는 (점수 80 이상 and 출석 90% and 과제 완료)
if score >= 90 or (score >= 80 and attendance >= 90 and assignment_done):
    grade = "A"
else:
    grade = "B"

print(f"학점: {grade}")
```

---

### 💻 실습 (10분)

**[실습 파일: session7_logical_practice.py](./session7_logical_practice.py)**

---

### ✅ 해설 (5분)

**[해설 파일: session7_logical_solution.py](./session7_logical_solution.py)**

---

## 세션 8: 연산자 우선순위 ★★★

### 📖 이론 (10분)

#### 개념 설명
연산자가 평가되는 순서입니다. **괄호를 사용**하면 명확하고 읽기 쉽습니다.

#### 주요 개념
- **우선순위**: `()` > `**` > `*,/` > `+,-` > 비교 > `not` > `and` > `or`
- **권장**: 괄호로 명확하게 표현

#### 실무 활용 사례
- 복잡한 할인 계산
- 다중 조건 검증
- 수식 계산

#### 코드 예시
```python
# 예시 1: 산술 연산 우선순위
result = 2 + 3 * 4  # 14 (곱셈 먼저)
result = (2 + 3) * 4  # 20 (괄호 먼저)

result = 10 / 2 * 3  # 15.0 (좌→우)
result = 10 / (2 * 3)  # 1.666...

# 예시 2: 논리 연산 우선순위
age = 25
score = 85
is_member = True

# and가 or보다 우선
result = age >= 18 and score >= 60 or is_member
# (age >= 18 and score >= 60) or is_member

# 명확하게 괄호 사용 (권장)
result = (age >= 18 and score >= 60) or is_member

# 예시 3: 실무 패턴 - 할인 계산
price = 100000
is_member = True
has_coupon = True

# 기본 할인
if price >= 100000:
    price = price * 0.95  # 5% 할인

# 회원 할인
if is_member:
    price = price * 0.9  # 추가 10% 할인

# 쿠폰 할인
if has_coupon:
    price = price - 5000

print(f"최종 금액: {price:,.0f}원")

# 권장: 괄호로 명확하게
if (age > 18 and score > 60) or (is_member and has_coupon):
    print("조건 만족")
```

---

### 💻 실습 (10분)

**[실습 파일: session8_precedence_practice.py](./session8_precedence_practice.py)**

---

### ✅ 해설 (5분)

**[해설 파일: session8_precedence_solution.py](./session8_precedence_solution.py)**

---

## 세션 9: 삼항 연산자 ★★★★

### 📖 이론 (10분)

#### 개념 설명
한 줄로 조건에 따라 값을 선택하는 표현식입니다. 간단한 조건에만 사용하세요.

**기본 문법**
```python
값1 if 조건 else 값2
```

#### 주요 개념
- **간결함**: 3줄 → 1줄
- **제한**: 복잡한 조건엔 부적합
- **가독성**: 단순할 때만 사용

#### 실무 활용 사례
- 상태 표시 (활성/비활성)
- 기본값 설정
- 간단한 분기

#### 코드 예시
```python
# 예시 1: 기본 사용
age = 25

# 일반 if문
if age >= 20:
    status = "성인"
else:
    status = "미성년자"

# 삼항 연산자 (한 줄)
status = "성인" if age >= 20 else "미성년자"
print(status)

# 예시 2: 다양한 활용
# 짝수/홀수
number = 7
parity = "짝수" if number % 2 == 0 else "홀수"

# 합격/불합격
score = 85
result = "합격" if score >= 60 else "불합격"

# 최대값 제한
value = 150
limited = value if value <= 100 else 100
print(limited)  # 100

# 예시 3: print 안에서 사용
score = 85
print(f"결과: {'합격' if score >= 60 else '불합격'}")

age = 17
print(f"입장: {'가능' if age >= 19 else '불가'}")

# ❌ 복잡하면 일반 if 사용
# 나쁜 예
grade = "A" if score >= 90 else "B" if score >= 80 else "C" if score >= 70 else "F"

# 좋은 예 (일반 if-elif 사용)
if score >= 90:
    grade = "A"
elif score >= 80:
    grade = "B"
elif score >= 70:
    grade = "C"
else:
    grade = "F"

# 실무 패턴: 배송비 계산
order_amount = 25000
shipping_fee = 0 if order_amount >= 30000 else 3000
print(f"배송비: {shipping_fee:,}원")

# 회원 포인트
is_member = True
points = int(order_amount * 0.05) if is_member else 0
print(f"적립 포인트: {points:,}P")
```

---

### 💻 실습 (10분)

**[실습 파일: session9_ternary_practice.py](./session9_ternary_practice.py)**

---

### ✅ 해설 (5분)

**[해설 파일: session9_ternary_solution.py](./session9_ternary_solution.py)**

---

## 📚 오늘의 핵심 정리

### 1. 컬렉션 자료형
```python
# 리스트 (수정 가능)
fruits = ["사과", "바나나"]
fruits.append("포도")

# 튜플 (수정 불가)
point = (10, 20)
x, y = point

# 딕셔너리 (키-값)
user = {"name": "김철수", "age": 28}
```

### 2. 타입 변환과 입력
```python
# 타입 변환
age = int(input("나이: "))
price = float(input("가격: "))

# f-string
print(f"{price:,}원")  # 천 단위
print(f"{pi:.2f}")     # 소수점 2자리
```

### 3. 연산자
```python
# 산술
10 / 3   # 3.333... (float)
10 // 3  # 3 (몫)
10 % 3   # 1 (나머지)

# 비교 (체이닝)
if 18 <= age < 65:
    print("성인")

# 논리
if age >= 18 and has_license:
    print("운전 가능")

# 삼항
status = "성인" if age >= 20 else "미성년자"
```

### Java vs Python
```python
# Java - 나눗셈
# int a = 10 / 3;  // 3 (정수)
# double b = 10.0 / 3;  // 3.333...

# Python - 나눗셈
a = 10 / 3   # 3.333... (항상 float)
b = 10 // 3  # 3 (정수 나눗셈)

# Java - 체이닝 불가
# if (age >= 18 && age <= 65)

# Python - 체이닝 가능
if 18 <= age <= 65:
```

---

## 🎯 다음 강의 예고

**3일차에서는:**
- if, elif, else (조건문)
- while 반복문
- break와 continue
- 무한 루프 활용

---

## ❓ FAQ

**Q1. input()은 왜 항상 문자열인가요?**
- 사용자가 입력하는 모든 것은 텍스트
- 숫자 계산이 필요하면 int() 또는 float()로 변환

**Q2. / 와 // 의 차이는?**
- `/`: 실수 나눗셈 (항상 float 반환)
- `//`: 정수 나눗셈 (몫만, 소수점 버림)

**Q3. 삼항 연산자는 언제 사용하나요?**
- 간단한 이진 선택 (2개 중 1개)
- 복잡하면 일반 if 사용 권장

**Q4. 리스트와 튜플 중 뭘 써야 하나요?**
- 수정 필요: 리스트
- 수정 불필요 (고정 데이터): 튜플
- 대부분은 리스트 사용

---

**강의 준비 완료! 화이팅!**
