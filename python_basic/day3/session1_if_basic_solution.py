"""
세션 1: if 문 기초 - 해설
난이도: ★★★★★
"""

# ============================================
# 문제 1: 관리자 권한 확인 (기초) - 해설
# ============================================
print("=" * 50)
print("문제 1: 관리자 권한 확인")
print("=" * 50)

role = "admin"

# ✅ 모범 답안
if role == "admin":
    print("관리자 권한이 있습니다.")

# 📌 주요 포인트:
# 1. == 연산자로 문자열 비교 (= 는 대입 연산자)
# 2. 들여쓰기 4칸 (탭 1개 또는 스페이스 4개)
# 3. 조건이 True일 때만 실행

# ⚠️ 자주 하는 실수:
# if role = "admin":  # ❌ = 대신 == 사용해야 함
# if role == "admin"  # ❌ 콜론(:) 빠뜨림
# print("관리자...")    # ❌ 들여쓰기 없음

# 💡 팁:
# - 문자열 비교 시 대소문자 구분 ("Admin" ≠ "admin")
# - 여러 역할 확인: if role in ["admin", "superuser"]:

print()


# ============================================
# 문제 2: 재고 부족 알림 (실무) - 해설
# ============================================
print("=" * 50)
print("문제 2: 재고 부족 알림")
print("=" * 50)

stock = 5

# ✅ 모범 답안
print(f"현재 재고: {stock}개")
if stock < 10:
    print("재고 부족! 발주가 필요합니다.")

# 📌 주요 포인트:
# 1. 비교 연산자 < 사용
# 2. f-string으로 변수 값 출력
# 3. 실무에서는 이메일 발송이나 알림 기능 추가 가능

# ⚠️ 자주 하는 실수:
# if stock =< 10:  # ❌ <= 순서 잘못됨
# if stock < "10":  # ❌ 숫자와 문자열 비교 오류

# 💡 팁:
# - 재고 임계값을 변수로 관리: THRESHOLD = 10
# - 로깅 기능 추가로 재고 변동 기록

print()


# ============================================
# 문제 3: 성인 인증 (나이 검증) - 해설
# ============================================
print("=" * 50)
print("문제 3: 성인 인증")
print("=" * 50)

age = 25

# ✅ 모범 답안
print(f"나이: {age}세")
if age >= 18:
    print("성인 인증 완료")

# 📌 주요 포인트:
# 1. >= 연산자 (크거나 같다)
# 2. 18세도 성인에 포함 (>= 사용)
# 3. 실무: 주민등록번호나 생년월일로 계산

# ⚠️ 자주 하는 실수:
# if age > 18:  # ❌ 18세가 제외됨 (>= 사용해야 함)
# if age >= "18":  # ❌ 문자열과 비교

# 💡 팁:
# - 청소년 구분: 13 <= age < 18
# - 연령대별 분류: if 20 <= age < 30:

print()


# ============================================
# 문제 4: 로그인 상태 확인 (불린 값) - 해설
# ============================================
print("=" * 50)
print("문제 4: 로그인 상태 확인")
print("=" * 50)

is_logged_in = True

# ✅ 모범 답안 (권장)
if is_logged_in:
    print("환영합니다!")

# 대안 (동일한 결과)
# if is_logged_in == True:  # 가능하지만 불필요하게 길음
#     print("환영합니다!")

# 📌 주요 포인트:
# 1. 불린 변수는 직접 사용 (if is_logged_in:)
# 2. == True는 불필요 (코드가 더 깔끔)
# 3. 변수명에 is_, has_ 접두사 사용 (가독성)

# ⚠️ 자주 하는 실수:
# if is_logged_in = True:  # ❌ = 대신 == (또는 생략)
# if "is_logged_in":  # ❌ 문자열은 항상 True

# 💡 팁:
# - 반대 조건: if not is_logged_in:
# - 여러 조건: if is_logged_in and is_verified:

print()


# ============================================
# 문제 5: 할인 쿠폰 적용 (실무 응용) - 해설
# ============================================
print("=" * 50)
print("문제 5: 할인 쿠폰 적용")
print("=" * 50)

price = 75000

# ✅ 모범 답안
print(f"원래 금액: {price:,}원")
if price >= 50000:
    print("10% 할인 쿠폰이 적용됩니다.")
    discounted_price = price * 0.9
    print(f"할인 후 금액: {discounted_price:,.0f}원")

# 📌 주요 포인트:
# 1. :, 로 천 단위 구분 기호
# 2. :.0f 로 소수점 제거
# 3. 할인율 계산: 0.9 곱하기 (또는 price - price * 0.1)

# ⚠️ 자주 하는 실수:
# discounted_price = price - 0.1  # ❌ 0.1원 할인됨 (10% 아님)
# print(f"{price:,}")  # ❌ :, 위치 잘못됨

# 💡 팁:
# - 할인율 변수로 관리: DISCOUNT_RATE = 0.1
# - 정수로 변환: int(price * 0.9)

print()


# ============================================
# 문제 6: 비밀번호 강도 체크 (길이 검증) - 해설
# ============================================
print("=" * 50)
print("문제 6: 비밀번호 강도 체크")
print("=" * 50)

password = "python1234"

# ✅ 모범 답안
print(f"비밀번호 길이: {len(password)}자")
if len(password) >= 8:
    print("안전한 비밀번호입니다.")

# 📌 주요 포인트:
# 1. len() 함수로 문자열 길이 확인
# 2. 8자 이상이 일반적인 비밀번호 최소 길이
# 3. 실무: 특수문자, 숫자 포함 여부도 확인

# ⚠️ 자주 하는 실수:
# if password >= 8:  # ❌ 문자열과 숫자 비교 불가
# if len(password) > 8:  # ❌ 8자도 포함해야 함 (>= 사용)

# 💡 팁:
# - 대문자 포함 확인: if any(c.isupper() for c in password):
# - 숫자 포함 확인: if any(c.isdigit() for c in password):
# - 실무에서는 정규표현식 사용

print()


# ============================================
# 🎯 핵심 정리
# ============================================
print("=" * 50)
print("🎯 세션 1 핵심 정리")
print("=" * 50)

print("""
1. if 문 기본 구조
   if 조건식:
       실행할 코드

2. 비교 연산자
   ==  같다
   !=  다르다
   >   크다
   <   작다
   >=  크거나 같다
   <=  작거나 같다

3. 불린 변수 사용
   ✅ if is_logged_in:  (권장)
   ❌ if is_logged_in == True:  (불필요)

4. 자주 하는 실수
   - == 대신 = 사용
   - 콜론(:) 빠뜨리기
   - 들여쓰기 잘못하기
   - > 와 >= 혼동

5. 실무 팁
   - 변수명은 의미있게 (is_, has_ 등)
   - 매직 넘버 대신 상수 사용
   - 조건식은 간결하게 작성
""")

print("=" * 50)
print("✅ 해설 완료!")
print("=" * 50)
