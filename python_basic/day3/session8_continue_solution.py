"""
세션 8: continue 문 - 해설
난이도: ★★★
"""

# ============================================
# 문제 1: 홀수만 출력 (기초) - 해설
# ============================================
print("=" * 50)
print("문제 1: 홀수만 출력")
print("=" * 50)

num = 0

# ✅ 모범 답안
while num < 20:
    num += 1

    if num % 2 == 0:  # 짝수면 건너뛰기
        continue

    print(num, end=' ')
print()  # 줄바꿈

# 📌 주요 포인트:
# 1. continue는 현재 반복만 건너뜀
# 2. 짝수 조건에서 continue 실행
# 3. continue 이후 코드는 실행 안됨
# 4. 다음 반복으로 즉시 이동

# ⚠️ 자주 하는 실수:
# if num % 2 == 0:
#     num += 1  # ❌ continue 전에 num += 1 하면 무한 루프
#     continue
#
# num = 0
# while num < 20:  # ❌ num += 1이 continue 후에 있으면 무한 루프
#     if num % 2 == 0:
#         continue
#     print(num)
#     num += 1

# 💡 팁:
# - 홀수 조건으로: if num % 2 == 1:
# - continue 없이: num += 2 (2씩 증가)

print()


# ============================================
# 문제 2: 3의 배수 제외하고 합계 - 해설
# ============================================
print("=" * 50)
print("문제 2: 3의 배수 제외하고 합계")
print("=" * 50)

num = 0
total = 0

# ✅ 모범 답안
while num < 30:
    num += 1

    if num % 3 == 0:  # 3의 배수는 건너뛰기
        continue

    total += num

print(f"3의 배수를 제외한 합: {total}")

# 📌 주요 포인트:
# 1. 3의 배수일 때 continue
# 2. continue 이후 total += num 건너뜀
# 3. 3의 배수가 아닐 때만 합산

# ⚠️ 자주 하는 실수:
# if num % 3 != 0:  # ⚠️ 가능하지만 continue 의미 없음
#     total += num
#     continue  # 의미 없는 continue

# 💡 팁:
# - 검증: 1+2+4+5+7+8+...+29 = 315
# - continue 없이: if num % 3 != 0:

print()


# ============================================
# 문제 3: 음수 제외하고 평균 (데이터 정제) - 해설
# ============================================
print("=" * 50)
print("문제 3: 음수 제외하고 평균")
print("=" * 50)

scores = [85, -1, 92, 78, -5, 88, 95, 0]
index = 0
total = 0
count = 0

# ✅ 모범 답안
print("유효한 점수: ", end='')

while index < len(scores):
    score = scores[index]
    index += 1

    if score < 0:  # 음수는 건너뛰기
        continue

    print(score, end=' ')
    total += score
    count += 1

print()  # 줄바꿈

if count > 0:
    average = total / count
    print(f"평균: {average:.2f}점")
else:
    print("유효한 점수가 없습니다.")

# 📌 주요 포인트:
# 1. 리스트 순회하며 음수 필터링
# 2. continue로 잘못된 데이터 제외
# 3. 유효한 데이터만 집계
# 4. 0으로 나누기 방지

# ⚠️ 자주 하는 실수:
# if score < 0:
#     index += 1  # ❌ 이미 증가했으므로 불필요
#     continue

# 💡 팁:
# - for 문 사용: for score in scores:
# - 리스트 컴프리헨션: valid = [s for s in scores if s >= 0]

print()


# ============================================
# 문제 4: 특정 문자 제외하고 출력 - 해설
# ============================================
print("=" * 50)
print("문제 4: 특정 문자 제외하고 출력")
print("=" * 50)

text = "Hello World"
result = ""
index = 0

# ✅ 모범 답안
while index < len(text):
    char = text[index]
    index += 1

    if char.lower() in ['a', 'e', 'i', 'o', 'u']:  # 모음 제외
        continue

    result += char

print(result)

# 📌 주요 포인트:
# 1. 문자열도 리스트처럼 순회 가능
# 2. .lower()로 대소문자 무시
# 3. in 연산자로 모음 확인
# 4. 모음 아닌 문자만 누적

# ⚠️ 자주 하는 실수:
# if char in ['a', 'e', 'i', 'o', 'u']:  # ❌ 대문자 모음 제외 안됨
# if char == 'a' or 'e' or 'i':  # ❌ 잘못된 조건

# 💡 팁:
# - vowels = "aeiouAEIOU"로 간단히
# - for 문: for char in text:

print()


# ============================================
# 문제 5: 로그 파싱 (에러 제외) - 해설
# ============================================
print("=" * 50)
print("문제 5: 로그 파싱")
print("=" * 50)

logs = [
    "INFO: System started",
    "ERROR: Connection failed",
    "INFO: User login",
    "ERROR: Database timeout",
    "INFO: Process completed"
]
index = 0

# ✅ 모범 답안
while index < len(logs):
    log = logs[index]
    index += 1

    if "ERROR" in log:  # ERROR 포함 시 건너뛰기
        continue

    print(log)

# 📌 주요 포인트:
# 1. in 연산자로 문자열 포함 여부 확인
# 2. ERROR 로그는 continue로 건너뜀
# 3. INFO 로그만 출력

# ⚠️ 자주 하는 실수:
# if log == "ERROR":  # ❌ 정확히 일치해야 함 (in 사용)
# if "ERROR" in log.lower():  # ⚠️ ERROR가 소문자면 못찾음

# 💡 팁:
# - 여러 레벨 필터링: if "ERROR" in log or "WARNING" in log:
# - 정규표현식으로 복잡한 패턴
# - ERROR 로그는 별도 파일에 저장

print()


# ============================================
# 문제 6: 범위 밖 데이터 필터링 - 해설
# ============================================
print("=" * 50)
print("문제 6: 범위 밖 데이터 필터링")
print("=" * 50)

temperatures = [25, -10, 35, 150, 42, 28, -5, 55]
index = 0
total = 0
count = 0

# ✅ 모범 답안
print("정상 온도: ", end='')

while index < len(temperatures):
    temp = temperatures[index]
    index += 1

    if temp < 0 or temp > 100:  # 범위 밖은 건너뛰기
        continue

    print(temp, end=' ')
    total += temp
    count += 1

print()  # 줄바꿈

if count > 0:
    average = total / count
    print(f"평균 온도: {average:.2f}도")

# 대안: not 사용
# if not (0 <= temp <= 100):
#     continue

# 📌 주요 포인트:
# 1. or 조건으로 범위 밖 확인
# 2. 정상 범위만 집계
# 3. 이상치(outlier) 제거 기법

# ⚠️ 자주 하는 실수:
# if temp < 0 and temp > 100:  # ❌ 동시에 만족 불가능
# if 0 <= temp <= 100:  # ⚠️ 가능하지만 continue 의미 반대
#     continue

# 💡 팁:
# - 범위 상수화: MIN_TEMP = 0, MAX_TEMP = 100
# - 통계: 표준편차 활용한 이상치 탐지

print()


# ============================================
# 문제 7: 주말 제외하고 근무일 수 계산 - 해설
# ============================================
print("=" * 50)
print("문제 7: 주말 제외하고 근무일 수 계산")
print("=" * 50)

day = 0
work_days = 0

# ✅ 모범 답안
print("주말: ", end='')

while day < 30:
    day += 1

    if day % 7 == 0:  # 7의 배수는 주말 (토요일)
        print(f"{day}일", end=', ' if day < 28 else '')
        continue

    work_days += 1

print(f"\n총 근무일: {work_days}일")

# 📌 주요 포인트:
# 1. 7의 배수를 주말로 가정
# 2. 주말은 continue로 건너뜀
# 3. 평일만 work_days 증가

# ⚠️ 자주 하는 실수:
# if day % 7 == 0:
#     work_days += 1  # ❌ continue 전에 증가하면 안됨
#     continue

# 💡 팁:
# - datetime 모듈로 실제 요일 계산
# - 공휴일도 제외하기
# - 주말: 토요일, 일요일 (6, 7의 배수)

print()


# ============================================
# 문제 8: 비밀번호 필터링 - 해설
# ============================================
print("=" * 50)
print("문제 8: 비밀번호 필터링")
print("=" * 50)

passwords = ["abc", "python123", "1234", "secure2024", "pass", "password!@#"]
index = 0
safe_count = 0

# ✅ 모범 답안
while index < len(passwords):
    password = passwords[index]
    index += 1

    if len(password) < 8:  # 8자 미만은 건너뛰기
        continue

    print(f"안전한 비밀번호: {password}")
    safe_count += 1

print(f"총 {safe_count}개의 안전한 비밀번호")

# 📌 주요 포인트:
# 1. 길이 조건으로 필터링
# 2. 조건 미달은 continue
# 3. 안전한 비밀번호만 카운트

# ⚠️ 자주 하는 실수:
# if len(password) >= 8:  # ⚠️ 가능하지만 continue 의미 없음
#     print(password)
#     continue

# 💡 팁:
# - 추가 조건: 숫자 포함, 특수문자 포함
# - any() 함수: any(c.isdigit() for c in password)
# - 여러 조건 동시 검사

print()


# ============================================
# 🎯 핵심 정리
# ============================================
print("=" * 50)
print("🎯 세션 8 핵심 정리")
print("=" * 50)

print("""
1. continue의 역할
   - 현재 반복을 건너뜀
   - 다음 반복으로 즉시 이동
   - 반복문은 계속 실행

2. break vs continue
   break:    반복문 완전 종료
   continue: 현재 반복만 건너뛰고 계속

3. continue 사용 패턴

   # 패턴 1: 조건 제외
   while 조건:
       if 건너뛸_조건:
           continue
       처리_코드

   # 패턴 2: 데이터 필터링
   while index < len(data):
       item = data[index]
       if 유효하지_않음:
           continue
       유효한_데이터_처리

4. 카운터 증가 위치 중요!
   ✅ 올바른 위치
   while num < 10:
       num += 1  # continue 전에
       if num % 2 == 0:
           continue
       print(num)

   ❌ 잘못된 위치
   while num < 10:
       if num % 2 == 0:
           continue
       num += 1  # continue 후면 증가 안됨 → 무한 루프

5. 실무 활용 사례
   - 데이터 정제 (이상치 제거)
   - 로그 필터링 (특정 레벨만)
   - 유효성 검증 (조건 미달 제외)
   - 특정 패턴 건너뛰기

6. 데이터 필터링 예시
   # 음수 제외
   if value < 0:
       continue

   # 범위 밖 제외
   if value < min or value > max:
       continue

   # 특정 문자 제외
   if char in excluded_chars:
       continue

7. continue 없이 작성하기
   # continue 사용
   while num < 10:
       num += 1
       if num % 2 == 0:
           continue
       print(num)

   # if로 대체
   while num < 10:
       num += 1
       if num % 2 == 1:
           print(num)

8. 언제 continue 사용?
   ✅ 예외 케이스가 간단할 때
   ✅ 필터링 로직이 명확할 때
   ✅ 들여쓰기 줄이고 싶을 때

   ❌ 조건이 복잡할 때 (if-else 권장)
   ❌ continue가 너무 많을 때

9. 중첩 루프에서 continue
   while 외부:
       while 내부:
           if 조건:
               continue  # 내부 while만 영향

10. 자주 하는 실수
    ❌ 카운터 증가를 continue 후에 배치
    ❌ 불필요한 continue (마지막 줄)
    ❌ continue 남용 (가독성 저하)

11. 최적화 팁
    # 빠른 체크를 먼저
    if simple_check():
        continue
    complex_operation()

    # 여러 조건은 or/and로
    if cond1 or cond2 or cond3:
        continue

12. for vs while에서 continue
    # for 문에서
    for item in items:
        if skip_condition:
            continue
        process(item)

    # while 문에서
    index = 0
    while index < len(items):
        item = items[index]
        index += 1  # 먼저 증가!
        if skip_condition:
            continue
        process(item)

13. 디버깅 팁
    - continue 전후에 print 추가
    - 카운터 증가 위치 확인
    - 무한 루프 걸리면 Ctrl+C
    - continue 조건 단순화

14. 코드 리팩토링
    # 중복 continue
    if cond1:
        continue
    if cond2:
        continue

    # 합치기
    if cond1 or cond2:
        continue
""")

print("=" * 50)
print("✅ 해설 완료!")
print("=" * 50)
